def transform(chain):
    chain1 = chain[0].strip(",")
    chain2 = chain[1].strip(",")
    
    return chain1[4],chain1[2],chain1[0] ": nom_prenom_classe"    
    
liste = ["1,3,4,7,13", "1,2,4,13,15"]

transform(liste)
