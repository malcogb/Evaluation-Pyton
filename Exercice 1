def transform(chain):
    chain1 = liste[0].split(",")
    chain2 = liste[0].split(",")
    
    arr1 =[]
    arr3 = []
    
    for i in range(len(chain1),2) :
        arr1.append(i)
        print(arr1)
    
liste = ["1,3,4,7,13", "1,2,4,13,15"]

transform(liste)
